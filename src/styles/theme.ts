interface Theme {
  bodyColor: string;
  buttonColor: string;
  black: string;
  white: string;
}

export const theme: Theme = {
  bodyColor: "#320071",
  buttonColor: "#E7B463",
  white: "#fff",
  black: "#000",
};
